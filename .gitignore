# ============================================================================
# OpenArm Lab — .gitignore
# 项目：Isaac Lab 快递盒翻面（标签朝上）RL 训练框架
# 规则：只忽略"生成物 / 缓存 / 本机文件"，源代码、资产源文件、文档一律入库。
# 说明：assets/usd/ 下的 USD 是 URDF→USD 转换缓存，可随时重新生成（见
#       README「ROBOT_SOURCE=urdf 回退路径」），故不入库。
# ============================================================================

# ---------------------------------------------------------------------------
# 训练输出 / 日志（体积大、可再生，一律不入库）
#   logs/rsl_rl/openarm_flip*/<时间戳>/ 下存放 model_*.pt checkpoint 与
#   TensorBoard events；需要分享某个训练好的模型时，请用 Git LFS 或
#   GitHub Releases 单独发布，不要提交进仓库。
# ---------------------------------------------------------------------------
logs/
*.pt
*.pth
*.onnx
*.tflite

# ---------------------------------------------------------------------------
# URDF -> USD 转换缓存（由脚本按需重新生成，体积大）
# ---------------------------------------------------------------------------
assets/usd/

# ---------------------------------------------------------------------------
# Isaac Sim / Isaac Lab 运行时产物
# ---------------------------------------------------------------------------
# URDF 导入器 / kit 输出的临时 USD 与子进程缓存
outputs/
_isaac_sim/
# kit/omni 日志（若在项目内运行过 AppLauncher）
*.kit_log
omni.log

# ---------------------------------------------------------------------------
# Python 缓存
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
build/
dist/
*.egg-info/
.eggs/

# ---------------------------------------------------------------------------
# 测试 / 静态分析缓存
# ---------------------------------------------------------------------------
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
coverage.xml

# ---------------------------------------------------------------------------
# 本地采集数据 / 临时截图（若在此项目内跑过 recorder / --shot）
# ---------------------------------------------------------------------------
datasets/
tmp/
*.npy
*.npz

# ---------------------------------------------------------------------------
# 编辑器 / 系统文件
# ---------------------------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ---------------------------------------------------------------------------
# 环境 / 凭据
# ---------------------------------------------------------------------------
.env
.env.*
!.env.example
